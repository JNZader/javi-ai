kind: package-catalog
contract_version: 0.1.0
status: published
packages:
  - id: shared.instructions
    type: shared
    version: 0.1.0
    dependencies: []
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: canonical shared instruction contract published before extraction
  - id: shared.agents
    type: shared
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: shared agent assets stay reusable across first-class providers
  - id: shared.skills
    type: shared
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: shared skill assets stay reusable across first-class providers
  - id: shared.hooks
    type: shared
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: shared hook assets stay reusable across first-class providers
  - id: shared.commands
    type: shared
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: shared command assets stay reusable across first-class providers
  - id: shared.mcp
    type: shared
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: shared MCP assets stay reusable across first-class providers
  - id: shared.memory
    type: shared
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
      - target.opencode.user
      - target.gemini.user
      - target.qwen.user
      - target.codex.user
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: shared memory assets stay reusable across first-class providers
  - id: provider.claude.core
    type: provider
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.claude.user
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: provider core package identifier for Claude-specific runtime assets
  - id: provider.opencode.core
    type: provider
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.opencode.user
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: provider core package identifier for OpenCode-specific runtime assets
  - id: provider.gemini.core
    type: provider
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.gemini.user
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: provider core package identifier for Gemini-specific runtime assets
  - id: provider.qwen.core
    type: provider
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.qwen.user
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: provider core package identifier for Qwen-specific runtime assets
  - id: provider.codex.core
    type: provider
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.codex.user
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: provider core package identifier for Codex-specific runtime assets
  - id: provider.copilot.core
    type: provider
    version: 0.1.0
    dependencies:
      - shared.instructions
    install_targets:
      - target.copilot.repo
    source_lineage:
      publication: net-new
      implementation_sources:
        - upstream
        - delta
      notes: provider core package identifier for Copilot-specific runtime assets
required_package_ids:
  - shared.instructions
  - shared.agents
  - shared.skills
  - shared.hooks
  - shared.commands
  - shared.mcp
  - shared.memory
  - provider.claude.core
  - provider.opencode.core
  - provider.gemini.core
  - provider.qwen.core
  - provider.codex.core
  - provider.copilot.core
package_contract:
  required_fields:
    - id
    - type
    - version
    - dependencies
    - install_targets
    - source_lineage
  package_types:
    - shared
    - provider
  source_lineage_values:
    - upstream
    - delta
    - net-new
  consumer_rules:
    - consume published package IDs only
    - do not infer package composition from javi-ai internal directories
