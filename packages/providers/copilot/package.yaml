kind: ai-provider-profile
id: provider.copilot.core
provider_id: copilot
display_name: GitHub Copilot
version: 0.1.0
status: partial
owner: javi-ai
canonical_path: packages/providers/copilot
install_target: target.copilot.repo
contract_refs:
  provider_manifest: manifests/providers.yaml#copilot
  package_manifest: manifests/packages.yaml#provider.copilot.core
  target_manifest: manifests/targets.yaml#target.copilot.repo
composition:
  required_packages:
    - shared.instructions
  optional_shared_packages:
    - shared.agents
    - shared.skills
    - shared.hooks
    - shared.commands
    - shared.mcp
    - shared.memory
homes:
  runtime: packages/providers/copilot/runtime
  overrides: packages/providers/copilot/overrides
  targets: packages/providers/copilot/targets
lineage:
  upstream_inputs:
    - upstream/gentle-ai
  delta_inputs:
    - delta/javi-dots
    - delta/agentes-ia
    - delta/project-starter-framework
  reference_legacy_sources:
    - vault/Javi.Dots/skills/setup.sh
    - vault/Javi.Dots/unified-instructions/orchestrator.md
  active_legacy_dependency: none
  cutover_status: wi-027-complete
  extraction_status: slice-wi-019
  extracted_assets:
    - targets/repo-profile.yaml
scope:
  owns:
    - provider-specific runtime config and wrappers for GitHub Copilot
    - provider-specific overrides, themes, permissions, and UX settings
    - install-target assembly inputs for target.copilot.repo
  excludes:
    - shared instructions, agents, skills, hooks, commands, MCP, and memory packages
    - bootstrap wrappers owned by javi-dots
    - project-scaffolding assets owned by javi-forge
notes:
  - WI-019 records the first Copilot target slice as a repo-scoped generated install recipe.
  - WI-027 retires active legacy derivation for the extracted Copilot assets; listed legacy paths are historical reference only.
  - Shared behavior remains canonical in shared packages even when Copilot adds local target transforms.
