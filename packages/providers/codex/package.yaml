kind: ai-provider-profile
id: provider.codex.core
provider_id: codex
display_name: Codex CLI
version: 0.1.0
status: partial
owner: javi-ai
canonical_path: packages/providers/codex
install_target: target.codex.user
contract_refs:
  provider_manifest: manifests/providers.yaml#codex
  package_manifest: manifests/packages.yaml#provider.codex.core
  target_manifest: manifests/targets.yaml#target.codex.user
composition:
  required_packages:
    - shared.instructions
  optional_shared_packages:
    - shared.agents
    - shared.skills
    - shared.hooks
    - shared.commands
    - shared.mcp
    - shared.memory
homes:
  runtime: packages/providers/codex/runtime
  overrides: packages/providers/codex/overrides
  targets: packages/providers/codex/targets
lineage:
  upstream_inputs:
    - upstream/gentle-ai
  delta_inputs:
    - delta/javi-dots
    - delta/agentes-ia
    - delta/project-starter-framework
  reference_legacy_sources:
    - vault/Javi.Dots/skills/setup.sh
    - vault/Javi.Dots/unified-instructions/orchestrator.md
  active_legacy_dependency: none
  cutover_status: wi-027-complete
  extraction_status: slice-wi-019
  extracted_assets:
    - targets/user-profile.yaml
scope:
  owns:
    - provider-specific runtime config and wrappers for Codex CLI
    - provider-specific overrides, themes, permissions, and UX settings
    - install-target assembly inputs for target.codex.user
  excludes:
    - shared instructions, agents, skills, hooks, commands, MCP, and memory packages
    - bootstrap wrappers owned by javi-dots
    - project-scaffolding assets owned by javi-forge
notes:
  - WI-019 records the first Codex target slice as a generated install recipe instead of duplicating shared instructions.
  - WI-027 retires active legacy derivation for the extracted Codex assets; listed legacy paths are historical reference only.
  - Shared behavior remains canonical in shared packages even when Codex adds local header or target transforms.
